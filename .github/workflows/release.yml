name: release

# 打 tag（v*）即发布：三平台原生构建（含 cgo → FFI 可用）+ 版本注入 + 变更日志 + 校验清单。
on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      version:
        description: '版本号（如 2.0.0；留空则用 VERSION 文件）'
        required: false

# 最小权限：工作流级只读；**只有 release 作业**额外拿 contents: write（创建 Release / 传资产）。
# 构建作业（三平台）不写仓库，因此不需要写权限。
permissions:
  contents: read

# 发布不并发、不取消：两次 tag 同时来时排队，避免把进行中的发布打断
concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    name: 构建（${{ matrix.os }}）
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4（SHA 固定；dependabot 周更）
        with:
          fetch-depth: 0   # 变更日志需要完整历史与 tag

      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5（SHA 固定；dependabot 周更）
        with:
          go-version: '1.26'

      - name: 版本号
        id: ver
        shell: bash
        run: |
          if [ -n "${{ github.event.inputs.version }}" ]; then
            v="${{ github.event.inputs.version }}"
          elif [ "${GITHUB_REF_TYPE}" = "tag" ]; then
            v="${GITHUB_REF_NAME#v}"
          else
            v="$(cat VERSION)"
          fi
          echo "version=$v" >> "$GITHUB_OUTPUT"
          echo "版本: $v"

      - name: 测试（解释器 + 编译器）
        shell: bash
        run: |
          go test ./... -count=1
          (cd compiler && go test ./... -count=1)

      - name: 构建全部工具（本机平台，cgo 开启 → FFI 可用）
        shell: bash
        run: |
          set -euo pipefail
          V="${{ steps.ver.outputs.version }}"
          ext=""; [ "${RUNNER_OS}" = "Windows" ] && ext=".exe"
          case "${RUNNER_OS}-${RUNNER_ARCH}" in
            Linux-X64)    plat="linux-amd64" ;;
            Linux-ARM64)  plat="linux-arm64" ;;
            macOS-X64)    plat="darwin-amd64" ;;
            macOS-ARM64)  plat="darwin-arm64" ;;
            Windows-X64)  plat="windows-amd64" ;;
            Windows-ARM64) plat="windows-arm64" ;;
            *) plat="unknown" ;;
          esac
          mkdir -p out
          for spec in "quark:." "qkcheck:./cmd/qkcheck" "qkdoc:./cmd/qkdoc" "qkrepl:./cmd/qkrepl" "qklsp:./cmd/qklsp"; do
            name="${spec%%:*}"; pkg="${spec##*:}"
            go build -trimpath -ldflags "-s -w -X main.version=$V" -o "out/${name}-${V}-${plat}${ext}" "$pkg"
          done
          (cd compiler && go build -trimpath -ldflags "-s -w -X main.version=$V" -o "../out/qkc-${V}-${plat}${ext}" .)
          # 无版本号别名：便于 README 里的固定一行命令（releases/latest/download/<工具>-<平台>）
          for f in out/*-${V}-${plat}${ext}; do
            alias="$(echo "$(basename "$f")" | sed "s/-${V}-/-/")"
            cp "$f" "out/$alias"
          done
          echo "构建完成："; ls -la out

      - name: 校验清单（sha256）
        shell: bash
        run: |
          V="${{ steps.ver.outputs.version }}"
          { echo "# QuarkLang ${V} 发布产物（${RUNNER_OS}/${RUNNER_ARCH}，cgo 开启）"; } > "out/MANIFEST-${V}-${RUNNER_OS}.txt"
          for f in out/*; do
            case "$f" in *MANIFEST*) continue;; esac
            printf '%s  %s\n' "$(sha256sum "$f" | cut -d' ' -f1)" "$(basename "$f")" >> "out/MANIFEST-${V}-${RUNNER_OS}.txt"
          done
          cat "out/MANIFEST-${V}-${RUNNER_OS}.txt"

      - name: 冒烟（跑一个示例 + --version）
        shell: bash
        run: |
          V="${{ steps.ver.outputs.version }}"
          ext=""; [ "${RUNNER_OS}" = "Windows" ] && ext=".exe"
          bin="$(ls out/quark-*-$V-*${ext} 2>/dev/null | head -1 || true)"
          [ -z "$bin" ] && bin="$(ls out/quark-*${ext} | head -1)"
          chmod +x "$bin" || true
          "./$bin" --version
          "./$bin" examples/hello.qk
          chmod +x out/qkcheck-*${ext} || true
          "$(ls out/qkcheck-*${ext} | head -1)" examples/ || true

      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4（SHA 固定；dependabot 周更）
        with:
          name: quarklang-${{ steps.ver.outputs.version }}-${{ runner.os }}
          path: out/*
          if-no-files-found: error
          retention-days: 30

  release:
    name: 发布（GitHub Release + 变更日志）
    needs: build
    runs-on: ubuntu-latest
    # 人工闸门：发布前必须由 environment "release" 的必需审阅人批准（admin 旁路已关闭）
    environment: release
    # 唯一需要写权限的作业：创建 Release、上传资产（其余作业只有 contents: read）
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4（SHA 固定；dependabot 周更）
        with:
          fetch-depth: 0

      - name: 版本号
        id: ver
        run: |
          if [ -n "${{ github.event.inputs.version }}" ]; then
            v="${{ github.event.inputs.version }}"
          elif [ "${GITHUB_REF_TYPE}" = "tag" ]; then
            v="${GITHUB_REF_NAME#v}"
          else
            v="$(cat VERSION)"
          fi
          echo "version=$v" >> "$GITHUB_OUTPUT"

      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v4（SHA 固定；dependabot 周更）
        with:
          path: artifacts

      - name: 生成变更日志
        run: |
          chmod +x scripts/changelog.sh
          ./scripts/changelog.sh "${{ steps.ver.outputs.version }}" > RELEASE_NOTES.md
          cat RELEASE_NOTES.md

      - name: 汇总产物
        run: |
          mkdir -p release
          find artifacts -type f -exec cp {} release/ \;
          ls -la release | head -50

      - name: 创建 Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          tag="v${{ steps.ver.outputs.version }}"
          if [ "${GITHUB_REF_TYPE}" = "tag" ]; then tag="${GITHUB_REF_NAME}"; fi
          gh release create "$tag" \
            --title "QuarkLang ${tag}" \
            --notes-file RELEASE_NOTES.md \
            --target "${GITHUB_SHA}" \
            release/* 2>/dev/null || \
          gh release upload "${tag}" release/* --clobber
