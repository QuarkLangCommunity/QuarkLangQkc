# QuarkLang v2 定案模型（2026-08-30 汇总，待设计者最终确认）

> 本文档汇总近期全部设计决定，是整体重写的依据。标注 ⚠ 者为尚未定死的点。
>
> **历史存档（2026-08-30）**：语法现已统一为**正典语法（类型在前）**，本文代码示例已按正典语法改写；权威语法清单见主工作区 `SYNTAX.md`，现行规范见 `spec.md`。

## 1. 类型体系（FuncBuffer 彻底删除）

- `void` = 空接口 `interface{}` 的**默认名字**（语言预声明 `interface {} void;`；"不是空，是没有接口"）。以后可直接写 `void` 表示空接口。
- `interface { ... }`：**匿名接口**（可作类型标注）；`type interface { ... } 名字;`：实名接口（必须写 `type`）。
- `struct { ... }`：**匿名结构体类型**；`type struct { ... } 名字;`：实名结构体（必须写 `type`）。
- 结构体字面量：`.{field: value, ...}`（字段名允许 `in`/`out` 等关键字）。
- `impl { ... } 类型;`：**唯一形态**（接口结构化满足，impl 上不写接口名）。
- 泛型 `type struct<T> { ... } Box;` / `impl<T> { ... } Box;`（struct 有泛型参数时 impl 必须引入）；指针 `T&` 与 `null`（自动解引用）；`Copyd<T>`（传递复制，`.ptr()` 取包装值）。以上保留。
- `List<T>`：滚动双指针缓冲（`*` 取开头、`next()`、`head()==tail()` 耗尽报错、`reset()` 回卷）。

## 2. 函数

- **必须声明返回类型**：`fn f(int n) int { ... }`（main 可省略 = void）。
- `return expr;`：返回并结束函数。
- `log expr;`：记录日志并结束函数（返回任意值，即 void 亦可）。
- **无 out、无多返回值**；多输出用 `List<T>` 作为单一返回值。调用直接得值。

## 3. 错误处理

- `try { ... } catch (类型 名字) { ... }`：catch 必须写类型和名字（如 `catch (void e)`）；错误信息装入 e。

## 4. 签名（Sign）

- `f(args) @instance(prefix)` ≡ `instance.call(prefix)(.{in: ..., out: ...})`（instance 为任意 Sign 实例变量名）：
  - instance 是 Sign 实例，名字任意（`类名 memo = 类名::new();`；类方法齐全即满足 Sign 接口——结构化满足，`impl` 上不写接口名）；
  - `instance.call(prefix)` 输出一个**函数**，该函数接收 `.{in, out}` 两字段记录并返回结果；
  - memorize 按 in→out 记忆化。
- ⚠ c. call 返回的函数如何执行被包装的原函数 f（f 引用从哪来）——待定。

## 5. 任务（taskm 全局变量）

- `taskm.spawn()`：参数为空，创建线程并直接返回 pid。
- `taskm.merge(pid, fn, args...)`：把函数并入线程 pid 执行。
- `taskm.block(pid)`：返回 void（空接口）——只等待，结果另走通道。
- `taskm.done(pid)`：该线程是否空闲（没有函数占用）。
- `taskm.channel([n])`：默认容量 1024；IO 执行表 FIFO、读优先。

## 6. 内存

- block 分配（`memory.setBlock(n)` 调粒度）、脏标记、协程结束自动标记可回收、`compact()` 实际清理（不返回）。

## 7. 宏系统（命名参数宏，定案）

- 定义：`#macro name (p1, p2, ...) { 主体 }`——参数在 `()` 内逗号分隔、个数不限；参数列表与主体分隔符 `()`/`[]`/`{}` 任意互换。
- 调用：`name(args)` / `name[args]` / `name{args}`——与定义同形；实参个数与形参一致；主体内形参按名替换为实参 token（宏体单趟展开，不递归）。
- 主体动态预处理：`#when (compile|run) { ... }` 按态选块；`#error("...")` 报错。
- 旧 pattern 语法（`macro {模式}{主体}` 及 `#insert/#ast/#execute`）废除。
- 预制宏：`program main;` / `program library;` / `pub` / `import ...;` 保留。

## 8. 待确认

1. ⚠ c. 签名 call 如何执行被包装函数 f。
2. 宏系统属于本次 v2 整体重写范围，还是 v2 核心先落地、宏系统随后一轮实现？
