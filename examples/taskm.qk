fn work(int n) int {
    int s = 0;
    int i = 0;
    while (i < 100000) {
        s = s + i;
        i = i + 1;
    }
    return s + n;
}
fn main(IOStream io) {
    thread t = taskm.spawn();
    t.merge(work, 1);
    taskm.block(t.pid());
    io.println(taskm.done(t.pid()));
}
